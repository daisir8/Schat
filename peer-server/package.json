{
  "name": "chatroom-peer-server",
  "version": "1.0.0",
  "description": "PeerJS signaling server for the serverless chatroom (signaling only, no chat content)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "peer": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
